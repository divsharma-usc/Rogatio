var questionid="#question"+<%=@questionid%>
var buttonid="#button_"+<%=@questionid%>
$(buttonid).unbind('click',false);
$(questionid).append("<%= escape_javascript (render 'newans',questionid: @questionid)%>");